# npm registry 配置
registry=https://registry.npmjs.org/

# 发布时使用的 registry（确保发布到官方 npm）
# 如果使用私有 registry，可以在这里配置
# @junxuan:registry=https://registry.npmjs.org/

# 发布访问权限（public 表示公开包）
access=public

# 认证 token（不要提交到 git，建议使用环境变量或 npm login）
# //registry.npmjs.org/:_authToken=${NPM_TOKEN}
